[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/green-api/*"
  to = "https://api.green-api.com/:splat"
  status = 200
  force = true
